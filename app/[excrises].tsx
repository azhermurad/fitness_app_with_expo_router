import { View, Text } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';

const ExcriseDetail = () => {
    return (
        <SafeAreaView >
            <Text>ExcriseDetail</Text>
        </SafeAreaView>
    );
};

export default ExcriseDetail;
